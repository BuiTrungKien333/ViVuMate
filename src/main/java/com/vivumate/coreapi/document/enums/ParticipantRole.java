package com.vivumate.coreapi.document.enums;

/**
 * Role of a participant within a GROUP conversation.
 * ADMIN: can manage members, edit group info, pin messages.
 * MEMBER: standard participant.
 */
public enum ParticipantRole {
    ADMIN,
    MEMBER
}
